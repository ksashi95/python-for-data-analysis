# 04 — Pandas Fundamentals

## Goal
Build the core DataFrame skills used in Python data analysis.

## What to Learn
1. Series
2. DataFrames
3. Loading CSV data
4. Loading Excel data
5. Inspecting datasets
6. Selecting columns and rows
7. Filtering data
8. Sorting data
9. Creating columns
10. Renaming columns
11. Removing rows and columns
12. Aggregation
13. `groupby()`
14. `value_counts()`
15. `unique()`
16. `nunique()`

## Important Inspection Methods
```python
df.head()
df.tail()
df.shape
df.columns
df.info()
df.describe()
df.dtypes
df.nunique()
```

## Practice
Use small datasets to answer questions such as:
- How many rows and columns are there?
- Which categories exist?
- What is the average value?
- Which category has the highest sales?
- How many unique customers are there?

## Completion Goal
You should be comfortable loading, inspecting, filtering, grouping, and summarizing a dataset with Pandas.
